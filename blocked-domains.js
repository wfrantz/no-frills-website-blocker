// List the domains you want to block below. All subdomains (e.g. www.example.com,
// mobile.example.com) will also be blocked.
export default `
example.com
example.net
example.org
`.trim().split(/\s+/);
